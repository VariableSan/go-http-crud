version: "3.8"

tasks:
  help:
    desc: Display help for each task
    cmds:
      - echo "Usage:"
      - echo "  task <target>"
      - echo ""
      - task --list

  run:
    desc: run for API
    cmds:
      - go run main.go -port 8081

  dev:
    cmds:
      - air
    desc: "Run live reload with Air"
